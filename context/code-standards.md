# Code Standards — .github

This repo contains no application code. Standards apply only to:
- YAML workflow files (lint.yml): standard GitHub Actions format
- Markdown: consistent with org style (ZVS 2018 orthography, SOV, ergative `in`)
